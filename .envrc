# Activate ESP-IDF environment (local installation)
export IDF_PATH="${PWD}/.esp-idf"
export IDF_TOOLS_PATH="${PWD}/.espressif"

if [ -f "$IDF_PATH/export.sh" ]; then
    source_env "$IDF_PATH/export.sh"
fi

# Activate Python venv managed by uv (for helper scripts)
layout python3
